# The ONLY packages the MEA-Analysis environment does not already have.
# Use this when installing into that environment (the recommended setup):
#
#   pip install -r requirements-web.txt
#
# Nothing here pulls in numpy, h5py or matplotlib, so it cannot disturb the
# pinned scientific stack the pipeline depends on.
fastapi>=0.110
uvicorn[standard]>=0.27

# Reports (slides). HTML reports need nothing extra.
python-pptx>=0.6.23

# Optional: the written summary in reports. Without it, and without
# ANTHROPIC_API_KEY in the environment, reports are built from the measured
# values alone and the UI explains why the summary is unavailable.
anthropic>=0.40
